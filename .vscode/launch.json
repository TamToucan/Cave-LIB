{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Cave Test",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/cave/cave_test.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Cave Test (Debug)",
            "sourceMap": {
                "build": "${workspaceFolder}",
                ".": "${workspaceFolder}"
            },
            "initCommands": [
                "settings set target.process.stop-on-sharedlibrary-events false",
                "settings set target.process.thread.step-in-avoid-nodebug false"
            ]
        }
    ]
}